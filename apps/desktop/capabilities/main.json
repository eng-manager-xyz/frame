{
  "identifier": "frame-desktop-windows",
  "description": "Allow only the versioned Frame bootstrap and fail-closed desktop dispatcher in the main window.",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "allow-bootstrap-main",
    "allow-bootstrap-desktop",
    "allow-dispatch-main"
  ],
  "platforms": ["macOS", "windows"]
}
