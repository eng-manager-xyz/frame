{
  "identifier": "frame-desktop-windows",
  "description": "Allow only Frame's versioned bootstrap and dispatcher in its three content-protected product windows; Instant finalize remains main-window only inside the Rust boundary.",
  "local": true,
  "windows": ["main", "overlay", "target-picker"],
  "permissions": [
    "allow-bootstrap-main",
    "allow-bootstrap-desktop",
    "allow-dispatch-main",
    "allow-finalize-instant"
  ],
  "platforms": ["macOS", "windows"]
}
