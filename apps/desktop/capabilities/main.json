{
  "identifier": "frame-desktop-windows",
  "description": "Allow only the typed Frame bootstrap command in the main window.",
  "local": true,
  "windows": ["main"],
  "permissions": ["allow-bootstrap-main"],
  "platforms": ["macOS", "windows"]
}
